# RobloxNoAgeRestrictionChat
This project allows the user to return to the a similar chat as the old style without being blocked by age groups, chat filter still applies

StarterPlayerScripts > LocalChat
/
ServerScriptService > ServerChatHierarchy
